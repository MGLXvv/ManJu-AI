# ManJu 图标分类清单

## iconfont-ready/cards
- `asset-star-outline` ← `Group 175.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `asset-trash` ← `Group 176.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `card-add` ← `Group 28.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `card-delete` ← `Group 20.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `card-edit` ← `Group 22.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `card-upload` ← `Group 21.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `star-green` ← `Group 246.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `star-orange` ← `Group 243.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `star-outline` ← `Group 245.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `star-purple` ← `Group 247.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/chips
- `chip-add` ← `Group 207.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `chip-close` ← `Frame-9.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `chip-close-active` ← `Frame-10.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `chip-close-muted` ← `Frame-11.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/editor-tools
- `reference-collapse-left` ← `Group 294.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `reference-expand-right` ← `Group 293.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-copy` ← `Group 216.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-copy-alt` ← `Group 216-1.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-delete` ← `Group 215.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-delete-alt` ← `Group 215-1.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-edit` ← `Group 212.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-edit-alt` ← `Group 212-1.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-fullscreen` ← `Group 248.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-lock` ← `Group 214.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-lock-alt` ← `Group 214-1.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-view` ← `Group 213.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-view-off` ← `Group 213-1.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-zoom` ← `Group 217.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `tool-zoom-out` ← `Group 217-1.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/flow
- `flow-complete` ← `Group 155.svg`：split icon from text label group
- `flow-script` ← `Group 152.svg`：split icon from text label group
- `flow-settings` ← `Group 153.svg`：split icon from text label group
- `flow-storyboard` ← `Group 156.svg`：split icon from text label group
- `flow-video` ← `Group 154.svg`：split icon from text label group

## iconfont-ready/forms
- `checkbox-checked` ← `Frame-16.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `checkbox-muted` ← `Frame-17.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/misc
- `ai-text-control` ← `Group 224.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `play-small` ← `Group 287.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `reference-rail-handle` ← `Group 188.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/navigation
- `topbar-cart-active` ← `Group 1-1.svg`：separated from credit/cart combined group
- `topbar-cart-default` ← `Group 1.svg`：separated from credit/cart combined group
- `topbar-home` ← `Group 2.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `topbar-points` ← `Group 6.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `topbar-resource` ← `Group 3.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `topbar-system` ← `Group 7.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `topbar-team` ← `Group 5.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `topbar-user` ← `Frame.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `topbar-voice` ← `Group 4.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/social
- `social-alipay` ← `Group 68.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `social-qq` ← `Group 66.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `social-wechat` ← `Group 67.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/timeline
- `pager-next` ← `Group 307.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `pager-prev` ← `Group 306.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `timeline-copy` ← `Frame-12.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `timeline-copy-active` ← `Frame-15.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `timeline-delete` ← `Group 296.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `timeline-delete-active` ← `Group 295.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `timeline-upload` ← `Frame-13.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `timeline-upload-active` ← `Frame-14.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/toolbar
- `action-delete` ← `Frame-8.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `action-document` ← `Frame-3.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `action-save` ← `Frame-6.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `action-template` ← `Frame-7.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `batch` ← `Group 17.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `chevron-down` ← `Frame-5.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `model-openai` ← `Frame-4.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `project-create-plus` ← `Frame 2.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `project-import` ← `Frame 3.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `search` ← `Frame-2.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## iconfont-ready/user-menu
- `user-menu-logout` ← `Frame-19.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `user-menu-notify` ← `Group 316.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `user-menu-password` ← `Frame-20.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `user-menu-space` ← `Frame-21.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable
- `user-menu-switch-team` ← `Frame-18.svg`：single-color iconfont candidate; wrappers/backgrounds stripped where applicable

## svg-only/complex
- `Frame-1` ← `Frame-1.svg`：not mapped in first necessary batch; review manually
- `Group 2-1` ← `Group 2-1.svg`：not mapped in first necessary batch; review manually
- `Group 3-1` ← `Group 3-1.svg`：not mapped in first necessary batch; review manually
- `Group 4-1` ← `Group 4-1.svg`：not mapped in first necessary batch; review manually
- `Group 5-1` ← `Group 5-1.svg`：not mapped in first necessary batch; review manually
- `Group 6-1` ← `Group 6-1.svg`：not mapped in first necessary batch; review manually
- `Group 7-1` ← `Group 7-1.svg`：not mapped in first necessary batch; review manually
- `空状态(1) 2` ← `空状态(1) 2.svg`：not mapped in first necessary batch; review manually

## svg-only/separated
- `topbar-credit-diamond-active` ← `Group 1-1.svg`：gradient/multicolor; use SVG, not iconfont
- `topbar-credit-diamond-default` ← `Group 1.svg`：gradient/multicolor; use SVG, not iconfont

## svg_only/components
- `Group 223` ← `Group 223.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 63` ← `Group 63.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 114` ← `Rectangle 114.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 115` ← `Rectangle 115.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 117` ← `Rectangle 117.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 19` ← `Rectangle 19.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 214` ← `Rectangle 214.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 41` ← `Rectangle 41.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 48` ← `Rectangle 48.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 48-1` ← `Rectangle 48-1.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 48-2` ← `Rectangle 48-2.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 48-3` ← `Rectangle 48-3.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 48-4` ← `Rectangle 48-4.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Rectangle 61` ← `Rectangle 61.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text

## svg_only/with_text_originals
- `Group 145` ← `Group 145.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 146` ← `Group 146.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 157` ← `Group 157.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 158` ← `Group 158.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 159` ← `Group 159.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 160` ← `Group 160.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 161` ← `Group 161.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 162` ← `Group 162.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 163` ← `Group 163.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 164` ← `Group 164.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 165` ← `Group 165.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 166` ← `Group 166.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 193` ← `Group 193.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 194` ← `Group 194.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 278` ← `Group 278.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text
- `Group 297` ← `Group 297.svg`：compound component/multicolor/contains text or background; keep SVG or rebuild with HTML text

## text-separated
- `AI优化` ← `AI优化.svg`：text label; use HTML/CSS text
- `Gpt.40` ← `Gpt.40.svg`：text label; use HTML/CSS text
- `topbar-credit-value` ← `Group 1.svg`：render as HTML text, not icon
- `上传` ← `上传.svg`：text label; use HTML/CSS text
- `主角家` ← `主角家.svg`：text label; use HTML/CSS text
- `保存` ← `保存.svg`：text label; use HTML/CSS text
- `全屏` ← `全屏.svg`：text label; use HTML/CSS text
- `删除` ← `删除.svg`：text label; use HTML/CSS text
- `删除-1` ← `删除-1.svg`：text label; use HTML/CSS text
- `删除-2` ← `删除-2.svg`：text label; use HTML/CSS text
- `团队空间` ← `团队空间.svg`：text label; use HTML/CSS text
- `夜晚街道` ← `夜晚街道.svg`：text label; use HTML/CSS text
- `导入项目` ← `导入项目.svg`：text label; use HTML/CSS text
- `展开` ← `展开.svg`：text label; use HTML/CSS text
- `已完成` ← `已完成.svg`：text label; use HTML/CSS text
- `已收藏` ← `已收藏.svg`：text label; use HTML/CSS text
- `已编辑` ← `已编辑.svg`：text label; use HTML/CSS text
- `已选` ← `已选.svg`：text label; use HTML/CSS text
- `批量` ← `批量.svg`：text label; use HTML/CSS text
- `搜索名称` ← `搜索名称.svg`：text label; use HTML/CSS text
- `收起` ← `收起.svg`：text label; use HTML/CSS text
- `文档` ← `文档.svg`：text label; use HTML/CSS text
- `新建项目` ← `新建项目.svg`：text label; use HTML/CSS text
- `未完成` ← `未完成.svg`：text label; use HTML/CSS text
- `未操作` ← `未操作.svg`：text label; use HTML/CSS text
- `未收藏` ← `未收藏.svg`：text label; use HTML/CSS text
- `未编辑` ← `未编辑.svg`：text label; use HTML/CSS text
- `未选` ← `未选.svg`：text label; use HTML/CSS text
- `未选中` ← `未选中.svg`：text label; use HTML/CSS text
- `未选中-1` ← `未选中-1.svg`：text label; use HTML/CSS text
- `模板` ← `模板.svg`：text label; use HTML/CSS text
- `正常操作` ← `正常操作.svg`：text label; use HTML/CSS text
- `添加` ← `添加.svg`：text label; use HTML/CSS text
- `添加-1` ← `添加-1.svg`：text label; use HTML/CSS text
- `禁止操作` ← `禁止操作.svg`：text label; use HTML/CSS text
- `空状态` ← `空状态.svg`：text label; use HTML/CSS text
- `管理充值` ← `管理充值.svg`：text label; use HTML/CSS text
- `系统管理` ← `系统管理.svg`：text label; use HTML/CSS text
- `编辑` ← `编辑.svg`：text label; use HTML/CSS text
- `编辑中` ← `编辑中.svg`：text label; use HTML/CSS text
- `花店` ← `花店.svg`：text label; use HTML/CSS text
- `视频播放` ← `视频播放.svg`：text label; use HTML/CSS text
- `资源库` ← `资源库.svg`：text label; use HTML/CSS text
- `选中` ← `选中.svg`：text label; use HTML/CSS text
- `选中-1` ← `选中-1.svg`：text label; use HTML/CSS text
- `选中可删除` ← `选中可删除.svg`：text label; use HTML/CSS text
- `音色播放按钮` ← `音色播放按钮.svg`：text label; use HTML/CSS text
- `音色管理` ← `音色管理.svg`：text label; use HTML/CSS text
- `首页` ← `首页.svg`：text label; use HTML/CSS text
